var exProgram = require("initing.js");

function nextPage() {
  if (exProgram == "powerPoint") {
    //bash
  } else {
    //bash
  }
}

function prevPage() {
  if (exProgram == "powerPoint") {
    //bash
  } else {
    //bash
  }
}

function startShow() {
  if (exProgram == "powerPoint") {
    //bash
  } else {
    //bash
  }
}

function stopShow() {
  if (exProgram == "powerPoint") {
    //bash
  } else {
    //bash
  }
}
